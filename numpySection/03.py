import numpy as np

# temps = np.array([19, 22, 25, 18, 27, 31, 16])

# moreThan25 = temps[temps > 25]

# indicesOfMoreThan25 = np.where(temps > 25)


# tempsA = temps[indicesOfMoreThan25]

# lenghtOfTemps = len(tempsA)

# revenues = np.array([1200, 1300, 1100, 2500, 2200])

# max=np.max(revenues)
# print(max)
# argmax = np.argmax(revenues)
# print(argmax)

# mean = np.mean(revenues)

# lenOfOverMean = len(revenues[revenues > mean])
# print(lenOfOverMean)

# upCount = 0
# maxChange = 0
# visits = np.array([100, 120, 90, 95, 150])

# diffs = np.diff(visits)
# up_count = np.sum(diffs > 0)
# max_change = np.max(np.abs(diffs))
# print(max_change)
# print(up_count)

# # for i in range(len(visits) - 1):
# #     diffrence = visits[i+1] - visits[i]
# #     if visits[i+1] > visits[i]:
# #         upCount += 1
# #     if diffrence > maxChange:
# #         maxChange = diffrence

# # print(upCount)
# # print(maxChange)